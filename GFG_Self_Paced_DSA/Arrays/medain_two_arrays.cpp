// Given two sorted arrays, find the median of the merged array (still sorted)
// Efficient Approach in O(log(n1)) , n1 <= n2 ( n1 = arr1.size(), and n2 = arr2.size() )

# include <bits/stdc++.h>
using namespace std ;

int median_finder ( vector <int> arr1 , vector <int> arr2 )
{
    
}